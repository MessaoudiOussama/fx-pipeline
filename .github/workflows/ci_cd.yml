name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ── 1. Test ────────────────────────────────────────────────────────────────
  test:
    name: Lint & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync --dev

      - name: Lint (ruff)
        run: uv run ruff check .

      - name: Run tests
        run: uv run pytest tests/ -v

  # ── 2. Deploy (only on push to main, after tests pass) ────────────────────
  deploy:
    name: Deploy to Azure Function App
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Azure Functions Core Tools
        run: npm install -g azure-functions-core-tools@4 --unsafe-perm true

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Restore Function App settings
        run: |
          STORAGE_CONN=$(az storage account show-connection-string \
            --name stfxpipeline09cf8a69 \
            --resource-group rg-fx-pipeline \
            --query connectionString --output tsv)

          az functionapp config appsettings set \
            --name func-fx-09cf8a69 \
            --resource-group rg-fx-pipeline \
            --settings \
              WEBSITE_CONTENTAZUREFILECONNECTIONSTRING="$STORAGE_CONN" \
              WEBSITE_CONTENTSHARE="func-fx-09cf8a69" \
              AzureWebJobsStorage="$STORAGE_CONN" \
              WEBSITE_RUN_FROM_PACKAGE=1

      - name: Deploy to Azure Functions
        run: func azure functionapp publish func-fx-09cf8a69 --python
